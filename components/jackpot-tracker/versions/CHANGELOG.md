# Jackpot Tracker (OSC) — version history

Each folder is a self-contained snapshot (own copy of tokens + assets) of `jackpot-tracker.osc.*`
at that point, kept so earlier states stay inspectable after later edits. The live/current version
always lives one level up, at `components/jackpot-tracker/jackpot-tracker.osc.*`.

- **v1-pink-cta-casino-logos** (2026-08-25) — CTA swapped to the real pink `.promo-code-cta-primary`
  button from `/bester-casino-bonus/` (uppercase, 3D bottom-shadow). Software-provider text kept,
  real casino operator logos added (own brand-colored chip per casino, from the same hub page).
